export enum TripStatus { New = 'New', Canceled = "Canceled", Finished = "Finished", inProgress = 'InProgress', DriverPath ='Driver Found' }

export enum TripType { Passenger = 'Passenger', Cargo = 'Cargo', ScheduledPassenger = 'Scheduled Passenger', ScheduledCargo = 'Scheduled Cargo' }